import {useEffect,useState} from 'react'; import {fulfillmentApi} from '../api/fulfillmentApi';
export default function Returns(){const [items,setItems]=useState([]);useEffect(()=>{fulfillmentApi.returns().then(setItems).catch(()=>setItems([]));},[]);return <main className="page-shell"><h1>Returns</h1>{!items.length?<div className="empty-card">No return requests yet.</div>:<div className="orders-list">{items.map(x=><article className="order-card" key={x.id}><h3>{x.productName}</h3><p>Order {x.orderNumber}</p><p>{x.reason}</p><span className="status-pill">{x.status}</span></article>)}</div>}</main>}
